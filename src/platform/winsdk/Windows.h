// Windows.h — case-variant alias for non-Windows builds.
// The sources include <Windows.h> (capitalised); on a case-sensitive filesystem
// that must resolve separately from <windows.h>. Both map to the same shim.
#include "windows.h"
