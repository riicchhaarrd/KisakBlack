// minwindef.h — portable stand-in. The real header defines the Win32 base scalar
// types; forward to our base-types header. (Deliberately no min/max macros — they
// poison the C++ standard library.)
#ifndef KISAK_MINWINDEF_H
#define KISAK_MINWINDEF_H
#include "_kisak_wintypes.h"
#endif
